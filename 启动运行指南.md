# 启动运行指南（从零到跑通）

> 这份指南解决一件事：**把项目跑起来，并且看到每个模块返回真实数据**（1000 名员工 / 72 个岗位，来自数据库）。
> 想改代码、加功能，看《队友开发指南.md》。

---

## 一、现在这个项目能做什么

五个模块都已经接上数据库，不再是写死的示例字符串：

| 模块 | 端口 | 现在能干什么 |
|---|---|---|
| 网关 talent-gateway | 9090 | 统一入口，按 `/api/模块/**` 路由，带 Sentinel 限流 |
| ① 员工档案 | 8081 | 分页查询（关键字/部门/岗位/标签/风险筛选）、详情（含薪酬、技能、培训）、增删改、部门统计、岗位下拉 |
| ② 梯队规划 | 8082 | 关键岗位继任候选人（按技能匹配度排序）、流失风险名单、岗位技能覆盖、Feign 跨服务取员工档案 |
| ③ 智能培训 | 8083 | 某员工的学习计划：岗位必修 - 已完成 = 待补课程 |
| ④ 晋升决策 | 8084 | 晋升候选人（跨层级 + 绩效达标 + 综合分排序） |
| ⑤ 数据分析 | 8085 | 人才盘点看板：人数、部门/岗位分布、人才标签、潜力、流失风险、绩效分布、平均薪酬/司龄/年龄 |
| 前端 talent-web | 5173 | 五个页面：人才看板、员工档案（查询/详情/增删改）、梯队规划、智能培训、晋升决策 |

---

## 二、环境要求

| 工具 | 版本 | 本机实测路径 |
|---|---|---|
| JDK 17 | 17+ | `D:\Develop\jdk17`（编译运行微服务） |
| JDK 8 | 8 | `D:\Develop\Java8`（**只用来启动 Nacos**） |
| Maven | 3.6+ | `D:\develop\apache-maven-3.6.1-bin\apache-maven-3.6.1\bin\mvn.cmd` |
| MySQL | 8.0 | 3306，账号 `root / 20050312` |
| Nacos Server | 2.4.0.1 | `D:\Develop\Nacos\nacos-server-2.4.0.1\nacos` |
| Sentinel 控制台 | 1.8.8 | `D:\Shixun\sentinel-dashboard-1.8.8.jar`（可选，没有也能跑业务） |

> 路径和别人不一样没关系，启动脚本的参数都能改，见第五节。

---

## 三、首次准备（每台机器只做一次）

### 1. 建库、建表、灌数据

```bat
cd /d D:\Shixun\Shixun2

rem 骨架表结构
cmd /c "D:\Develop\MySQL80\bin\mysql.exe -uroot -p20050312 --default-character-set=utf8mb4 < sql\talent_db.sql"

rem 1000 人 + 72 岗位的数据集（会重建 13 张数据表，脚本可重复执行）
cmd /c "D:\Develop\MySQL80\bin\mysql.exe -uroot -p20050312 --default-character-set=utf8mb4 < sql\talent_dataset.sql"

rem 技能树：给 skill 表加分类层级，把 172 个技能归到 9 个分类下（可重复执行）
cmd /c "D:\Develop\MySQL80\bin\mysql.exe -uroot -p20050312 --default-character-set=utf8mb4 < sql\talent_skill_tree.sql"

rem 数据分析与报告：模型日志、报表字段、人才流动记录（可重复执行）
cmd /c "D:\Develop\MySQL80\bin\mysql.exe -uroot -p20050312 --default-character-set=utf8mb4 < sql\talent_analysis.sql"
```

第二条命令跑完会自己打印每张表的条数，正常是 `emp_employee 1000`、`position 72`、`emp_skill 4515`……
第三条会打印 9 个分类各有多少个技能（合计 172）。
第四条会打印 6 条模型日志、1000 条入职事件、3 个报表模板。

> 执行顺序是 `talent_db.sql` → `talent_dataset.sql` → `talent_skill_tree.sql` → `talent_analysis.sql`。
> 如果重跑了 `talent_dataset.sql`，技能树会被重置成平表，记得再跑一次第三、第四条。

### 2. 编译并把 talent-common 装进本地仓库

```bat
mvn clean install -DskipTests
```

**这一步必须在项目根目录做**，否则别的模块会报 `Could not resolve com.talent:talent-common`。

---

## 四、启动（推荐一条命令）

```powershell
powershell -ExecutionPolicy Bypass -File scripts\start-all.ps1
```

它会依次做四件事，并汇报每一步：

1. 检查 JDK / Maven / MySQL 是否就绪；
2. 用 JDK 8 启动 Nacos（已经在跑就跳过）；
3. 启动 Sentinel 控制台（jar 不存在、或加了 `-SkipSentinel` 就跳过）；
4. 启动网关和 5 个业务服务，等端口起来后打印结果。

正常输出结尾长这样：

```
启动完成：6 / 6 个服务在跑
常用地址：
  网关入口      http://127.0.0.1:9090/api/employee/page?pageNum=1&pageSize=5
  人才看板      http://127.0.0.1:9090/api/analysis/dashboard
  Nacos 控制台  http://127.0.0.1:8848/nacos
  Sentinel      http://127.0.0.1:8858
```

**停止全部：**

```powershell
powershell -ExecutionPolicy Bypass -File scripts\stop-all.ps1
```

### 再起一个前端（可选）

前端在 `talent-web` 目录，Vue 3 + Vite + TypeScript + Element Plus。它通过 Vite 的代理
访问网关 9090，所以**后端必须先起来**。

```bash
cd talent-web
npm install        # 第一次才需要
npm run dev
```

浏览器打开 http://127.0.0.1:5173 。启动脚本不管前端，前端要单独开一个窗口跑。

> 想让同局域网的同学也能打开，用 `npm run dev -- --host`，然后把自己机器的 IP 发给他们。

---

## 五、手动启动（用 IDEA 或命令行）

不用脚本也行，顺序是：**MySQL → Nacos → Sentinel（可选）→ 网关 → 5 个服务**。

### 1. Nacos（必须用 JDK 8）

```bat
set JAVA_HOME=D:\Develop\Java8
cd /d D:\Develop\Nacos\nacos-server-2.4.0.1\nacos\bin
startup.cmd -m standalone
```

访问 http://127.0.0.1:8848/nacos ，账号 `nacos / nacos`。

### 2. Sentinel 控制台（可选）

```bat
java -Dserver.port=8858 -Dsentinel.dashboard.auth.username=sentinel -Dsentinel.dashboard.auth.password=sentinel -jar D:\Shixun\sentinel-dashboard-1.8.8.jar
```

访问 http://127.0.0.1:8858 ，账号 `sentinel / sentinel`。不启动它业务照常跑，只是看不到监控、配不了规则。

### 3. 六个服务

IDEA 里分别 Run 这 6 个启动类（Project SDK 和 Maven Runner 都选 JDK 17）：

| 启动类 | 端口 |
|---|---|
| `GatewayApplication` | 9090 |
| `EmployeeServiceApplication` | 8081 |
| `PlanningServiceApplication` | 8082 |
| `TrainingServiceApplication` | 8083 |
| `PromotionServiceApplication` | 8084 |
| `AnalysisServiceApplication` | 8085 |

命令行方式：

```bat
set JAVA_HOME=D:\Develop\jdk17
mvn spring-boot:run -pl talent-gateway
mvn spring-boot:run -pl talent-employee-service
rem ……其余同理
```

> **重要：服务跑起来之后不要再执行 `mvn install`**。Maven 会覆盖 `talent-common` 的 jar，
> 运行中的 JVM 再加载新类就会报 `NoClassDefFoundError: com/talent/common/...`。
> 真改了公共模块，编译完把服务重启一遍即可。

---

## 六、验证跑通了没有

按顺序执行，每条都能对上就说明整条链路是通的（都走网关 9090，和前端调用方式一致）。

### 1. 员工分页——应该返回 1000 条总数

```bat
curl "http://127.0.0.1:9090/api/employee/page?pageNum=1&pageSize=2"
```

```json
{"code":200,"message":"操作成功","data":{"total":1000,"pageNum":1,"pageSize":2,
 "records":[{"id":1,"empNo":"E70014679","name":"龙秀英","positionName":"首席执行官",
 "perfScore":2.00,"potentialLevel":"C级（待提升）","warningLevel":"低风险","talentTag":"待优化"}, ...]}}
```

### 2. 员工详情——带薪酬、技能、培训

```bat
curl "http://127.0.0.1:9090/api/employee/2"
```

看 `managerName`（上级姓名）、`skills`（技能标签）、`courses`（已完成培训）有没有值。

### 3. 人才盘点看板——所有数字都是现算的

```bat
curl "http://127.0.0.1:9090/api/analysis/dashboard"
```

应该看到 `totalEmployees:1000`、`positionCount:72`、`highRiskCount:6`、
`avgSalary:471450.26`、`avgTenure:5.1`、以及部门/标签/潜力/风险/绩效五组分布。

### 4. 培训推荐——岗位必修减去已完成

```bat
curl "http://127.0.0.1:9090/api/training/paths?employeeId=38"
```

```json
{"code":200,"data":{"employeeName":"袁梓豪","positionName":"人力资源经理",
 "completed":[{"courseName":"创新思维"}],
 "recommended":[{"courseName":"HRBP"},{"courseName":"人才盘点培训"}]}}
```

### 5. 梯队与晋升

```bat
curl "http://127.0.0.1:9090/api/planning/succession?positionId=1&limit=3"
curl "http://127.0.0.1:9090/api/planning/risk?limit=3"
curl "http://127.0.0.1:9090/api/promotion/candidates?positionId=1&limit=3"
```

继任候选会给出 `matchScore`（技能匹配度）；晋升候选会给出 `totalScore`（综合分）。

### 6. 跨服务调用（Feign）

```bat
curl "http://127.0.0.1:9090/api/planning/employee/2/profile"
```

梯队模块自己库里没有员工档案，这份数据是从 8081 的员工服务取的。把员工服务停掉再调，
会走降级返回 `{"code":503,"message":"员工服务暂时不可用，已降级返回；..."}`。

### 7. 统一异常格式

```bat
curl "http://127.0.0.1:9090/api/employee/99999"
```

```json
{"code":404,"message":"员工不存在：99999","data":null,"timestamp":...}
```

参数校验失败是 `400`（比如新增员工不传姓名），非法 JSON 也是 `400`，都不会再抛 500 堆栈。

### 8. 限流（Sentinel）

用 PowerShell 快速打 30 次，会看到前 20 次通过、后 10 次被拦：

```powershell
$c = New-Object System.Net.Http.HttpClient
1..30 | ForEach-Object { [int]$c.GetAsync('http://127.0.0.1:9090/api/employee/list').Result.StatusCode }
```

被拦的响应是 `{"code":429,"message":"请求过于频繁，请稍后再试"}`。
阈值在 `talent-gateway` 的 `SentinelGatewayRuleConfig` 里（现在是每秒 20 次），演示时可以调小。

### 9. 前端页面

浏览器打开 http://127.0.0.1:5173 ，应该看到：

- 左侧五个菜单，默认停在「人才看板」，卡片上显示 **在职员工 1000、岗位数 72、高风险人员 6**；
- 点「员工档案」能看到 1000 人的分页列表，点「详情」弹出该员工的薪酬、技能标签、已完成培训；
- 点「智能培训」，在员工框里输入「袁」搜到袁梓豪，能看到他已完成 1 门、待补「HRBP / 人才盘点培训」2 门。
- 点「技能体系」：左边是技能树（9 个分类、172 个技能），点分类看它下面的技能，点技能看「多少人会 / 多少岗位要求」。
- 员工档案页勾选几行，可以试「批量修改」「批量删除」；点「导出 Excel」下载当前筛选条件下的全部数据；
  「导入 Excel」里先下载模板，填好再传回来，错误的行会按行号告诉原因。

如果页面打开是空白，先看浏览器 F12 控制台：多半是后端没起（网关 9090 连不上）。

---

## 七、接口清单

所有接口都能直连（`8081~8085`）或走网关（`9090`），路径一致。响应统一是：

```json
{ "code": 200, "message": "操作成功", "data": ..., "timestamp": 1789455853795 }
```

### ① 员工档案 `8081`

| 方法 | 路径 | 参数 | 说明 |
|---|---|---|---|
| GET | `/api/employee/page` | `pageNum` `pageSize` `keyword` `department` `positionId` `talentTag` `warningLevel` `status` | 分页查询，pageSize 上限 200 |
| GET | `/api/employee/{id}` | — | 详情（含薪酬、技能、培训） |
| GET | `/api/employee/list` | — | 前 10 条，快速联调用 |
| POST | `/api/employee` | JSON body，`empNo` `name` `positionId` 必填 | 新增，返回新 id |
| PUT | `/api/employee/{id}` | JSON body | 修改 |
| DELETE | `/api/employee/{id}` | — | 删除（关联记录级联删除） |
| GET | `/api/employee/options/positions` | — | 岗位下拉 |
| GET | `/api/employee/options/departments` | — | 部门下拉 |
| GET | `/api/employee/stats/department` | — | 部门人数统计 |
| POST | `/api/employee/batch/delete` | body `{ids:[...]}` | 批量删除 |
| PUT | `/api/employee/batch/update` | body `{ids,department?,status?,workMode?,levelTier?,jobRank?}` | 批量修改，只改传了的字段 |
| GET | `/api/employee/export` | 与分页查询相同（都可选） | 导出 Excel，导出**筛选后的全部数据**而不只是当前页 |
| GET | `/api/employee/import-template` | — | 下载导入模板 |
| POST | `/api/employee/import` | multipart 的 `file` | 导入 Excel，返回成功/失败明细 |
| GET | `/api/employee/{id}/skills` | — | 这个员工会的技能 |
| PUT | `/api/employee/{id}/skills` | body `{skillIds:[...]}` | 给员工配技能（整体覆盖，只能选具体技能） |

### ①·附 技能树 `8081`（走网关 `/api/skill/**`）

| 方法 | 路径 | 参数 | 说明 |
|---|---|---|---|
| GET | `/api/skill/tree` | — | 整棵技能树（分类 + 子技能，带「多少人会 / 多少岗位要」） |
| GET | `/api/skill/categories` | — | 分类列表 |
| GET | `/api/skill/children` | `parentId` | 某个分类下的技能 |
| GET | `/api/skill/search` | `keyword` | 按技能名或分类名搜 |
| POST | `/api/skill` | body `{skillName,parentId?,description?,status?,sortOrder?}` | 新增，`parentId=0` 就是新增分类 |
| PUT | `/api/skill/{id}` | 同上 | 修改，也可以换分类 |
| DELETE | `/api/skill/{id}` | — | 删除；分类下有技能、或技能已被引用时会拒绝并说明原因 |

> 删除保护是刻意做的：被员工或岗位引用的技能删不掉，只能改成「停用」，避免历史数据对不上。

### ② 梯队 `8082`

| 方法 | 路径 | 参数 | 说明 |
|---|---|---|---|
| GET | `/api/planning/succession` | `positionId` `limit` | 继任候选人 + 匹配度 |
| GET | `/api/planning/risk` | `limit` | 流失风险名单（按分数倒序） |
| GET | `/api/planning/skill-coverage` | `positionId` | 该岗位核心技能的全公司覆盖人数 |
| GET | `/api/planning/employee/{id}/profile` | — | Feign 跨服务取员工档案 |

### ③ 培训 `8083`

| 方法 | 路径 | 参数 | 说明 |
|---|---|---|---|
| GET | `/api/training/paths` | `employeeId` | 学习计划（已完成 + 待补） |
| GET | `/api/training/records` | `employeeId` | 已完成的培训 |

### ④ 晋升 `8084`

| 方法 | 路径 | 参数 | 说明 |
|---|---|---|---|
| GET | `/api/promotion/candidates` | `positionId` `limit` | 晋升候选人 + 综合分 |

### ⑤ 分析 `8085`

| 方法 | 路径 | 参数 | 说明 |
|---|---|---|---|
| GET | `/api/analysis/dashboard` | — | 基础盘点：人数、部门/岗位分布、标签/潜力/风险/绩效分布 |
| GET | `/api/analysis/pipeline` | — | **梯队整体状态**：结构 + 质量 + 风险 + 健康度评分与管理建议 + 绩效×潜力九宫格 |
| GET | `/api/analysis/flow-trend` | — | **人才流动趋势**：历年/近 24 月入职趋势、各司龄段风险、按入职年份的人员画像、流动事件构成 |
| GET | `/api/analysis/model/effect` | — | **模型运行效果**：训练次数/成功率/平均与最好 AUC、分模型汇总、版本效果与耗时趋势 |
| GET | `/api/analysis/model/logs` | `modelName` `status` `version` `pageNum` `pageSize` | **模型日志溯源**：训练日志分页查询 |
| GET | `/api/analysis/model/logs/{id}` | — | 单次训练的全部信息：训练参数、评估指标、数据版本、备注 |
| GET | `/api/analysis/model/names` | — | 模型名列表（筛选下拉用） |
| GET | `/api/analysis/report/options` | — | 自定义报表可选的维度和指标 |
| POST | `/api/analysis/report/generate` | body `{reportName,dimensions[],metrics[],filters{},save}` | **生成自定义报表**，返回明细表 + 图表数据 + 溯源信息 |
| POST | `/api/analysis/report/from-template/{templateId}` | — | 按模板一键生成并存档 |
| GET | `/api/analysis/report/history` | — | 历史报表列表 |
| GET | `/api/analysis/report/{id}` | — | 报表详情（含存下来的明细数据，用于回看和溯源） |
| DELETE | `/api/analysis/report/{id}` | — | 删除报表 |
| GET | `/api/analysis/report/templates` | — | 报表模板列表 |
| POST | `/api/analysis/report/templates` | body 同 generate | 把当前配置存成模板 |
| DELETE | `/api/analysis/report/templates/{id}` | — | 删除模板 |

> 自定义报表的维度和指标走**白名单**（维度 12 个、指标 12 个），传了白名单外的 key 会直接返回
> `不支持的维度/指标`，值一律用参数占位，所以不会出现 SQL 注入。
> 每份报表都会记下生成时的**数据版本**和**模型版本**，配合模型日志可以回答"这个数字是哪份数据、哪个模型算出来的"。

---

## 八、代码怎么分层（照着加自己的接口）

以员工模块为例，一次请求的路径是：

```
浏览器/前端 → 网关 9090 → 业务服务 8081
                              └─ controller   接参数、返回 Result
                                 └─ service   业务规则、校验、事务
                                    └─ mapper   SQL（MyBatis-Plus）
                                       └─ MySQL  talent_db
```

每个模块自己的包结构（`talent-xxx-service/src/main/java/com/talent/xxx/`）：

| 目录 | 放什么 |
|---|---|
| `controller` | REST 接口，只做参数接收和 `Result.success(...)` 包装 |
| `service` / `service/impl` | 业务逻辑，校验失败抛 `BusinessException` |
| `mapper` | 继承 `BaseMapper` 做增删改，复杂查询用 `@Select` 注解写 SQL |
| `entity` | 一张表一个实体，`@TableName("表名")` |
| `dto` | 入参（查询条件、保存表单） |
| `vo` | 出参（列表行、详情、统计结果） |

`talent-common` 里已经有这些公共能力，**不要重复写**：

- `Result` / `PageResult` / `ResultCode`：统一返回格式
- `GlobalExceptionHandler`：统一异常处理（业务异常、参数校验、JSON 格式错误等）
- 自动配置：通过 `META-INF/spring/...AutoConfiguration.imports` 注册，各服务**不用加 `@ComponentScan`**
- MyBatis-Plus 分页插件：不装的话 `page()` 会把整表查出来

加一个新接口的完整步骤，参考员工分页：

1. `mapper` 里加一个 `@Select` 方法（需要分页就把 `IPage<T>` 作为第一个参数）；
2. `service` 里包一层，做校验、拼装（比如详情要再查技能和培训两张表）；
3. `controller` 加一个 `@GetMapping`，返回 `Result.success(...)`。

---

## 九、常见问题

**1. 报 `Could not resolve com.talent:talent-common`**
根目录执行过 `mvn clean install` 了吗？必须先装公共模块。

**2. 服务启动报 `NoClassDefFoundError: com/talent/common/...`**
几乎都是**在服务运行期间执行了 `mvn install`**，把 `talent-common` 的 jar 覆盖了。
重启对应服务即可（`scripts\stop-all.ps1` 再 `scripts\start-all.ps1`）。

**3. Nacos 起不来 / 报 JDK 相关错误**
Nacos 2.4.0.1 要用 **JDK 8** 启动，微服务才用 JDK 17。
本机 JDK 8 在 `D:\Develop\Java8`，注意别写成 `D:\develop\jdk`（那个是 JDK 17）。

**4. 端口冲突**

| 端口 | 用途 |
|---|---|
| 8081~8085 | 五个业务服务 |
| 9090 | 网关 |
| 8848 / 9848 | Nacos |
| 8858 | Sentinel 控制台 |
| **8719** | **Sentinel 控制台自己占着，各服务从 8720 开始编号** |

**5. Sentinel 控制台里看不到服务 / 监控是空的**
本机装了 VMware、Radmin VPN 等多张虚拟网卡，Sentinel 自动探测的 IP 可能是 `26.x` 这种，
控制台连不上。六个模块都已经固定 `spring.cloud.sentinel.transport.client-ip: 127.0.0.1`，
需要时用环境变量 `SENTINEL_CLIENT_IP` 覆盖。

**6. 用 PowerShell 发 POST 请求，中文变成 `????`、或者报 JSON 解析错误**
PowerShell 直接传 `-d '{"a":"中文"}'` 会丢引号、编码也不对。两种办法：

```powershell
# 办法一：用 Invoke-RestMethod
$body = @{ empNo='E90001'; name='测试'; positionId=3 } | ConvertTo-Json
Invoke-RestMethod -Uri http://127.0.0.1:9090/api/employee -Method Post -ContentType 'application/json' -Body $body

# 办法二：把 JSON 写进文件，让 curl 读文件
curl.exe -X POST -H "Content-Type: application/json" --data-binary "@body.json" http://127.0.0.1:9090/api/employee
```

**7. 接口突然返回 `{"code":429,"message":"请求过于频繁"}`**
被 Sentinel 限流了，等 1 秒就好。嫌阈值低就改
`talent-gateway` 的 `SentinelGatewayRuleConfig` 里的 `QPS` 常量，重启网关生效。

**8. 自己写的 `.ps1` 脚本中文乱码、报"缺少字符串结束符"**
Windows PowerShell 5.1 读没有 BOM 的 UTF-8 文件会按 GBK 解析。存成
**UTF-8 with BOM** 即可（项目里的 `scripts\*.ps1` 已经带 BOM）。

**9. 数据被改坏了想恢复**
重跑 `sql\talent_dataset.sql` 就会把 13 张数据表恢复成初始 1000 人 + 72 岗位的状态。

**10. Git 上 clone 下来怎么不是最新代码**
仓库里 `main` 和 `origin/master` 是两条互不相干的历史，而默认分支还指向 `master`。
拉代码时明确用 `git checkout main`，并建议在 Gitee 上把默认分支改成 `main`。

---

## 十、还没做的部分（队友可以接着做）

以下表还是空的，等对应模块补充数据和功能：

| 模块 | 空表 | 可以做什么 |
|---|---|---|
| ② 梯队 | `pos_succession_candidate` `pos_risk` `talent_pool` `resign_warning_rule` | 把继任候选/风险评估的结果落库、人才池容量测算、预警规则配置 |
| ③ 培训 | `learning_path` `path_task` | 多阶段学习路径（现在只做到"岗位必修 - 已完成"） |
| ④ 晋升 | `promo_candidate` `promo_case` `promo_assessment` | 候选人评分落库、历史案例库、评估报告 |
| ⑤ 分析 | `report_template` `report` `model_log` | 报表模板与生成、模型训练日志 |

另外两件可以做的事：把 Sentinel 规则持久化到 Nacos（现在是内存规则，重启会丢）；
`emp_employee.is_key`（关键岗位标识）还没标数据，标上之后梯队模块的关键岗位分析就能用。
