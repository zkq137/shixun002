# 智能企业人才梯队建设平台 —— 队友开发指南

> 请每位队友先通读本指南，再开始写自己的模块。核心原则：**你只负责自己的模块目录，公共部分别动。**

---

## 一、你要负责的模块

| 同学 | 模块 | 模块目录 | 端口 | 启动类 |
|---|---|---|---|---|
| 1 | 员工档案智能管理 | `talent-employee-service` | 8081 | `EmployeeServiceApplication` |
| 2 | 梯队规划与预测 | `talent-planning-service` | 8082 | `PlanningServiceApplication` |
| 3 | 智能培训推荐 | `talent-training-service` | 8083 | `TrainingServiceApplication` |
| 4 | 晋升决策支持 | `talent-promotion-service` | 8084 | `PromotionServiceApplication` |
| 5 | 数据分析与报告 | `talent-analysis-service` | 8085 | `AnalysisServiceApplication` |

其余公共部分：
- `talent-gateway`（网关，9090）：统一入口，路由已按 `/api/模块/**` 配好，**一般不用改**。
- `talent-common`：公共类（统一返回 `Result`、异常、分页），**不要改**。
- `pom.xml`（父工程）：版本管理，**不要改**（如有公共依赖需求，找组长）。
- Sentinel 限流熔断：已由组长统一接入（依赖、控制台地址、端口都配好了），
  你不用碰 pom 和配置；要给自己的接口加限流，按 README 第八节在控制台里配规则即可。

> 入口路径约定：你的控制器统一用 `@RequestMapping("/api/你的模块英文名")`，例如员工模块是 `/api/employee`。

---

## 二、环境准备（每人必做）

| 工具 | 版本 | 说明 |
|---|---|---|
| JDK | **17** | 编译/运行微服务；IDEA 的 Project SDK 和 Maven JDK 都选 17 |
| Maven | 3.6+ | 构建 |
| Nacos Server | 2.4.0.1 | 注册/配置中心，**用 JDK 8 启动** |
| MySQL | 8 | 数据存储，库 `talent_db` |

### 启动本地 Nacos（用 JDK 8）
```bat
rem JDK 8 的路径按自己机器改。注意别写成 D:\develop\jdk——本机那个目录是 JDK 17
set JAVA_HOME=D:\Develop\Java8
cd /d D:\Develop\Nacos\nacos-server-2.4.0.1\nacos\bin
startup.cmd -m standalone
```
启动后访问 `http://127.0.0.1:8848/nacos`（账号 `nacos / nacos`）。

### 初始化数据库（只做一次）
```bat
cmd /c "D:\Develop\MySQL80\bin\mysql.exe -uroot -p20050312 --default-character-set=utf8mb4 < sql\talent_db.sql"
```
> 不要重复导入，否则示例数据会重复。若 Nacos 用 MySQL 存配置，还需执行 Nacos 的 `conf/mysql-schema.sql`（先建 `nacos` 库）。

---

## 三、拉取代码 & 首次编译（关键）

```bat
git clone 你的仓库地址.git
cd Shixun2
git checkout -b feature/你的模块
```

**首次必须在项目根目录执行一次：**
```bat
mvn clean install
```
> 这一步会把 `talent-common` 装进你的本地 Maven 仓库。**不做这步，编译你的模块会报 `Could not resolve com.talent:talent-common`**。

---

## 四、启动并运行你的模块

### 方式一：IDEA（推荐）
1. `File → Open` 选择根目录的 `pom.xml`，以项目打开
2. `File → Project Structure → Project SDK = 17`
3. `Settings → Build Tools → Maven → Runner → JRE = 17`
4. 右键运行你自己的 `*Application`（见上面表格）

### 方式二：命令行
```bat
set JAVA_HOME=D:\Develop\jdk17
mvn spring-boot:run -pl talent-你的模块-service
```

### 方式三：一键启动全部（推荐）
```powershell
powershell -ExecutionPolicy Bypass -File scripts\start-all.ps1     # 启动 Nacos + 控制台 + 6 个服务
powershell -ExecutionPolicy Bypass -File scripts\stop-all.ps1      # 全部停掉
```
详细的启动步骤、验证清单、常见问题都在《启动运行指南.md》里。

启动后自动注册到 Nacos。验证：
- 你的服务直连：`http://127.0.0.1:8081/api/employee/list`
- 网关访问：`http://127.0.0.1:9090/api/employee/list`
- Nacos 控制台 **服务管理 → 服务列表** 里能看到你的服务，健康实例数为 1

---

## 五、数据库表和接口约定

共用 `talent_db` 时，**表前缀约定好**，避免重名：

| 模块 | 表前缀 |
|---|---|
| 员工 | `emp_`：`emp_employee` 基本信息、`emp_salary` 薪酬、`emp_performance` 绩效、`emp_potential` 潜力、`emp_skill` 技能、`emp_talent_tag` 人才标签 |
| 梯队 | `pos_` / `talent_pool` / `resign_`：`position` 岗位画像、`pos_skill_require` 岗位技能要求、`pos_course_require` 岗位必备培训、`resign_warning_record` 流失预警 |
| 培训 | `course` / `learning_path` / `path_task` / `training_record`：`course` 课程字典、`training_record` 培训记录 |
| 晋升 | `promo_` |
| 分析 | `report_` / `model_log` |
| 公共 | `skill` 技能字典、`course` 课程字典（员工侧和岗位侧共用，**不要往里塞重复的技能名**） |

1000 名员工 + 72 个岗位的数据已经灌好，表之间的关系和常用查询见 README 第九节。

**技能树**：`skill` 表是两层的——`parent_id = 0` 的是分类（9 个，如「研发技术」），挂在分类下的是具体技能
（172 个）。员工会哪些技能存在 `emp_skill`，岗位要求哪些技能存在 `pos_skill_require`，两边都引用 `skill.id`。
分类和技能的增删改查接口在员工模块里（`/api/skill/**`）。其他模块要用技能数据，调接口或直接查表都行，
但**新增、改名要走接口**，别自己往 `skill` 表里插，会绕过重名校验。

数据库连接在各模块 `src/main/resources/application.yml` 里配置（默认已给 `talent_db` / root / 20050312），按需改成自己的账号密码。**不要修改公共表（如员工基础表）**，跨模块取数据请通过 Feign 调用对方服务（参考 `talent-planning-service` 里的 `EmployeeClient`）。

---

## 六、代码分层约定（照着写就行）

一次请求的路径：`controller → service → mapper → MySQL`，每层只干自己的事：

| 目录 | 放什么 | 注意 |
|---|---|---|
| `controller` | REST 接口 | 只接参数、返回 `Result.success(...)`，不写业务 |
| `service` / `service/impl` | 业务逻辑、校验、事务 | 校验不过就 `throw new BusinessException("原因")` |
| `mapper` | 继承 `BaseMapper<T>` | 增删改用它现成的；多表查询用 `@Select` 注解写 SQL |
| `entity` | 一张表一个类 | 加 `@TableName("表名")` |
| `dto` | 入参 | 查询条件、表单提交 |
| `vo` | 出参 | 列表行、详情、统计结果 |

参考实现：`talent-employee-service` 里员工的分页 / 详情 / 增删改，是完整的一套。

**`talent-common` 里已经有了，别重复写：**

- 统一返回：`Result`、`PageResult`、`ResultCode`
- 统一异常处理：`GlobalExceptionHandler`（业务异常、参数校验、JSON 格式错误、缺参数都转成统一格式）
- 自动配置：靠 `AutoConfiguration.imports` 注册，**各服务不用加 `@ComponentScan`**（之前扫不到包的问题已经修好）
- MyBatis-Plus 分页插件：**没有它 `page()` 会把整张表查出来**，已经统一装好

**改完代码的注意点：**

- 只改了本模块的代码：重启这个服务即可。
- 改了 `talent-common`：必须 `mvn install` 后**重启所有服务**。服务还在跑时执行 `mvn install`，
  JVM 会加载不到新类，报 `NoClassDefFoundError: com/talent/common/...`。

---

## 七、开发时最容易踩的坑

1. **JDK 版本不对**：Boot 3.2 必须 JDK 17。用 8 编译启动必失败。
2. **`@PathVariable` 丢了名字**：写 `@PathVariable("id") Long id`，别只写 `@PathVariable Long id`（否则编译没 `-parameters` 时启动即报错）。
3. **没先 `mvn clean install`**：导致找不到 `talent-common`。
4. **Nacos 用 JDK 17 启动**：Nacos 2.4.0.1 用 JDK 8 启动，微服务才用 JDK 17。
5. **不改网关也能通**：网关按 `/api/模块/**` 路由好了，你只需保证自己服务的 `@RequestMapping` 前缀一致。
6. **端口别重复**：每个模块固定端口（8081~8085），不要改，否则注册不上或冲突。
7. **Sentinel 控制台要单独启动**（见 README 第八节）。没启动不影响业务，只是看不到监控、配不了规则。
8. **Sentinel 通信端口**已按模块分好（8720~8725），别改成默认的 8719——控制台自己占着这个端口。
9. **服务运行中执行 `mvn install`**：会把 `talent-common` 的 jar 换掉，运行中的服务随后报
   `NoClassDefFoundError`。重启服务就好，别去改代码。
10. **错误码要分清楚**：业务校验失败用 `400`（`ResultCode.VALIDATE_FAILED`），资源不存在用 `404`，
    不要一律 500，前端不好区分。
11. **PowerShell 里发 POST**：直接写 `-d '{"name":"张三"}'` 会丢引号、中文还会变 `????`，
    改用 `Invoke-RestMethod`，或把 JSON 写进文件再 `--data-binary "@文件"`。
12. **Excel 导出报 `NoSuchMethodError: ...UnsynchronizedByteArrayOutputStream.builder()`**：
    EasyExcel 用的 POI 需要 commons-io 2.12+，而 `spring-cloud-starter-openfeign` 会带 2.11.0。
    父工程已经把它统一锁成 2.16.1，别把这个版本管理去掉；自己引新库时留意别被传递依赖降级。

---

## 八、Git 协作流程

```bat
git add .
git commit -m "模块：完成xxx功能"
git push origin feature/你的模块
```
开发完成后，从 `main` 拉最新、解决冲突，再合并回 `main`（建议走 Gitee 的 Pull Request）。

**公共文件（pom.xml / talent-common / talent-gateway）只由组长改**，其他人提交时不要带上这些文件的改动，避免冲突。

---

## 九、前端（talent-web）

前端是 Vue 3 + Vite + TypeScript + Element Plus，五个模块的接口都已经接好，一个模块一个文件：

| 页面 | 页面文件 | 接口文件 | 建议负责人 |
|---|---|---|---|
| 人才看板 | `src/views/DashboardView.vue` | `src/api/analysis.ts` | ⑤ 分析 |
| 员工档案 | `src/views/EmployeeView.vue` | `src/api/employee.ts` | ① 员工 |
| 梯队规划 | `src/views/PlanningView.vue` | `src/api/planning.ts` | ② 梯队 |
| 智能培训 | `src/views/TrainingView.vue` | `src/api/training.ts` | ③ 培训 |
| 晋升决策 | `src/views/PromotionView.vue` | `src/api/promotion.ts` | ④ 晋升 |

每人只需要动自己那一页 + 对应的 api 文件，公共部分（`App.vue` 布局、`router/index.ts` 路由、
`api/request.ts` 请求封装）不要动，避免冲突。改了后端字段就先改 `src/api/types.ts`。

启动方式、接口约定、生产部署见 `talent-web/README.md`。
